def solution(board, k):
    answer = 0
    
    for i in range(len(board)):
        for j in range(len(board[i])):
            # i + j <= k일 경우 board[i][j] 합 반환
            if i + j <= k:
                answer += board[i][j]
                
    return answer
