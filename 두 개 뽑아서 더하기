def solution(numbers):
    answer = []
    
    for i in range(len(numbers)) :
        for j in range(len(numbers)) :
            # 중복이 아닌 경우
            if i != j :
                answer.append(numbers[i] + numbers[j])
    answer = sorted(set(answer))  # 오름차순
    
    return answer
