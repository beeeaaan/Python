def solution(arr, flag):
    answer = []
    
    for i in range(len(arr)):
        # flag[i]가 true일 경우
        if flag[i]:
            answer += [arr[i]] * (arr[i] *2)
        # flag[i]가 false일 경우
        else:
            answer = answer[:-arr[i]]
            
    return answer
