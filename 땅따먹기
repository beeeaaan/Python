def solution(land):
    # 현재 열 제외한 이전 행 최대값 더하기
    for i in range(1, len(land)):
        for j in range(len(land[0])):
            land[i][j] += max(land[i-1][:j] + land[i-1][j+1:])

    return max(land[len(land)-1])
