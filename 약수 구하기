def getMyDivisor(n):
    divisor = []

    # 제곱근 구하기
    for i in range(1, int(n ** (1/2)) + 1):
        if (n % i == 0):
            divisor.append(i) 
            if ( (i**2) != n) : 
                divisor.append(n // i)
                divisor.sort()
    
    return divisor
