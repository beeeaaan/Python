def solution(my_string, overwrite_string, s):
    # s 이전까지의 기존 문자열 + 덮어쓸 문자열 + 덮어쓴 뒤 남은 기존 문자열
    answer = my_string[:s] + overwrite_string + my_string[s + len(overwrite_string):]
    
    return answer
