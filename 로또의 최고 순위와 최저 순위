def solution(lottos, win_nums):
    rank = [6, 6, 5, 4, 3, 2, 1]
    count = lottos.count(0)
    answer = 0
    
    # 랭킹 계산하기
    for x in win_nums:
        if x in lottos:
            answer += 1
            
    return rank[count + answer], rank[answer]
