def solution(s):
    answer = ''
    s = s.split(' ')
    
    for i in range(len(s)):
        # capitalize 함수 : 첫 문자 알파벳일 경우 대문자로 변환, 두번째 문자부터 소문자
        # 첫 문자가 알파벳이 아닐 경우
        s[i] = s[i].capitalize()
    answer= ' '.join(s)
    
    return answer
