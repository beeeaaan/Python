def solution(ingredient):
    stack = []
    count = 0
    
    # 재료 추가
    for i in ingredient:
        stack.append(i)
        # 햄버거 완성 확인
        if stack[-4:] == [1, 2, 3, 1]:
            count += 1
            for _ in range(4):
                stack.pop()
                
    return count
