def solution(s, skip, index):
    # ASCII코드로 알파벳 배열 만들기(skip 들어가는 문자 제외)
    alphabet = [chr(i) for i in range(97, 123) if not chr(i) in skip] * 3
    answer = ''
    
    for i in s:
        # 새로운 알파벳으로 변환
        answer += alphabet[alphabet.index(i) + index]
        
    return answer
