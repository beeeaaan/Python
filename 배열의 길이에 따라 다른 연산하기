def solution(arr, n):
    # arr 길이가 홀수일 경우 짝수 인덱스 + n
    if len(arr) % 2 == 1:
        for i in range(0, len(arr), 2):
            arr[i] += n
    else:
        # arr 길이가 짝수일 경우 홀수 인덱스 + n
        for i in range(1, len(arr), 2):
            arr[i] += n
            
    return arr
