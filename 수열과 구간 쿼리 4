def solution(arr, queries):
    for s, e, k in queries:
        for i in range(s, e + 1):
            # i를 k로 나눈 나머지가 0일 경우 i가 k의 배수
            if i % k == 0:
                arr[i] += 1
                
    return arr
